@model CategoryVM
@{
    ViewBag.Title = "CategoryList";
}



<section class="panel">
    <header class="panel-heading">
        DataTables hidden row details example
    </header>
    <div class="panel-body">
        <div class="adv-table">
            <table cellpadding="0" cellspacing="0" border="0" class="display table table-bordered" id="hidden-table-info">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Kategori Ismi</th>
                        <th class="hidden-phone">Acıklama</th>
                        <th>Durum</th>
                        <th class="hidden-phone">Sil</th>
                        <th class="hidden-phone">Guncelle</th>
                       
                    </tr>
                </thead>
                <tbody>

                    @foreach (Category item in Model.Categories)
                    {

                        <tr class="gradeX">
                            <td>@item.ID</td>
                            <td>
                                @item.CategoryName
                            </td>
                            <td class="hidden-phone">@item.Description</td>
                            <td>@item.Status</td>
                            <td class="center hidden-phone">@Html.ActionLink("Sil","DeleteCategory",new { id = item.ID},new { @class="btn btn-danger"})</td>
                            <td class="center hidden-phone">@Html.ActionLink("Güncelle", "DeleteCategory", new { id = item.ID }, new { @class = "btn btn-warning" })</td>
                        </tr>
                    }



                </tbody>
            </table>

        </div>
    </div>
</section>


<script src="~/Areas/Admin/AdminScripts/AdminCategoryDTJ.js"></script>